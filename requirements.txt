# YNAB API client
ynab-sdk>=0.3.0

# Environment variable management
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Email parsing (for Amazon and Venmo transaction emails)
imapclient>=2.3.1
pyzmail36>=1.0.5

# HTML parsing (for parsing email content)
beautifulsoup4>=4.12.0

# Data handling
python-dateutil>=2.8.2

# YAML parsing (for category rules)
pyyaml>=6.0

# AI classification (for LLM fallback)
anthropic>=0.40.0
